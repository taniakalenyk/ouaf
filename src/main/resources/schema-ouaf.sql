CREATE TABLE IF NOT EXISTS quote_of_the_day (
                                                id INT PRIMARY KEY,
                                                quote TEXT NOT NULL
);

CREATE TABLE IF NOT EXISTS quote_of_the_day (
                                                id INT PRIMARY KEY,
                                                quote TEXT NOT NULL
);